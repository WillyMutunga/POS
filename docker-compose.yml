version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: sparks_bytes_db
    restart: always
    environment:
      POSTGRES_DB: lightspos
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: William#20
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
